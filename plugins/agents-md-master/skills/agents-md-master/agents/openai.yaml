interface:
  display_name: "AGENTS.md Master"
  short_description: "Audit AGENTS.md systems, conflicts, and semantic duplication"
  default_prompt: "Use $agents-md-master to audit the effective AGENTS.md chain for semantic duplication, contradictions, deletion candidates, density, writing quality, and linked guidance without editing files."

policy:
  allow_implicit_invocation: true
